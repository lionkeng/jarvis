{
  "name": "@jarvis-viz/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch run src/index.ts",
    "start": "bun run dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "build": "bun build ./src/index.ts --target=bun --outdir=dist"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
