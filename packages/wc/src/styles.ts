export const voiceVizElementStyles = `
  :host {
    display: block;
    contain: content;
    min-width: 0;
    min-height: 180px;
  }

  .mount {
    width: 100%;
    height: 100%;
    min-height: inherit;
    box-sizing: border-box;
    color: #d5e2e5;
    font: normal 16px/1.5 ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    letter-spacing: normal;
    text-transform: none;
  }

  .mount * { box-sizing: border-box; }
`;
